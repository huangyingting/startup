# gh-aw lock files are generated from the .md sibling by `gh aw compile`.
# Mark them so review tooling treats them as generated and merges keep ours.
.github/workflows/*.lock.yml linguist-generated=true merge=ours
