{
  "name": "startup-scripts",
  "private": true,
  "type": "module",
  "scripts": {
    "check:workflow-config": "node .agents/skills/startup-research/scripts/config.mjs",
    "check:report-index": "node .agents/skills/startup-research/scripts/index.mjs --strict --check",
    "check:reports": "npm --prefix website run check:reports",
    "validate": "npm run check:workflow-config && npm run check:report-index && npm run check:reports && npm --prefix website run build"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  }
}
