{
  "name": "startup-scripts",
  "private": true,
  "type": "module",
  "scripts": {
    "build:report-index": "node scripts/build-report-index.mjs",
    "build:evidence-ledger": "node scripts/build-evidence-ledger.mjs",
    "check:chapter": "node scripts/check-chapter-readiness.mjs",
    "check:report-index": "node scripts/build-report-index.mjs --strict --check",
    "check:reports": "npm --prefix website run check:reports",
    "validate": "npm run check:report-index && npm run check:reports && npm --prefix website run build"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  }
}
