{
  "name": "startup-scripts",
  "private": true,
  "type": "module",
  "scripts": {
    "check:workflow-config": "node .agents/skills/startup-research/scripts/config.mjs",
    "check:report-index": "node .agents/skills/startup-research/scripts/index.mjs --strict --check",
    "build:report-index": "node .agents/skills/startup-research/scripts/index.mjs --strict",
    "validate": "npm run check:workflow-config && npm run check:report-index && npm --prefix website run build"
  },
  "dependencies": {
    "js-yaml": "^4.1.0",
    "pdfjs-dist": "^5.7.284"
  }
}
